Documentación para el tercer entregable del PIA Programación Básica:
En la lectura de datos se ingresó un archivo de texto que fue llenado por el anterior script, para leer los datos obtenidos se abrió el archivo de texto permitiendo leer caracteres especiales y recorriendo línea por línea con un bucle, se recorrió buscando el inicio de cada consulta guardada en el archivo de texto usando expresiones regulares para identificar este inicio. Las expresiones regulares consisten en 3 grupos, el primer acepta caracteres que representan el lugar del cual se están encontrando los terremotos, los otros 2 admitían dígitos separados por guiones para encontrar fechas en el archivo de texto, ya que cada consulta está delimitada por dos fechas. 
Para validar los datos de cada consulta se usaron de nuevo expresiones regulares, está vez buscando coincidencias con dígitos que pueden o no estar separados por un punto para encontrar las magnitudes de cada terremoto y un segundo grupo que admite dígitos que representan el número de terremotos con esa magnitud. 
Al encontrar una línea de magnitud se toma el valor del primer grupo como un flotante, el segundo grupo como un entero y se añade la magnitud a una lista de magnitudes encontradas. 
Los datos obtenidos con las expresiones regulares se ingresaron a un diccionario con claves para la región, las fechas de inicio y final, las magnitudes y claves para el análisis estadístico. 
Para el análisis estadístico se usaron los módulos numpy y statistics, para calcular la moda y la media de las magnitudes encontradas. También se añadió un apartado de las frecuencias de las magnitudes usando un .count. 
Por último se imprimieron apartados de fechas de inicio y final, región, media, moda y una lista de las magnitudes por frecuencia, para cada consulta encontrada. Este orden será usado después para las gráficas. 
